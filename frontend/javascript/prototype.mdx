---
title: '111'
sidebar_position: 1
---

> 原型与原型链是 `javaScript` 中非常重要的知识。下面我会简单的来分析一下它们。

```js
class Person {

}
var person = new Person();
person.n = 'wangly19';
console.log(person.name)

```

如上代码，我们都知道ES6中的`Class API`本质上也是一个对象的扩展，在 `ES5` 的时候，也经常会通过 `Function` 的方法来实现对应的效果。

先做结果总结(面试): 

## prototype

每个函数都有一个 prototype 属性，就是我们经常在各种例子中看到的那个 prototype ，比如：


## __proto__


## constructor